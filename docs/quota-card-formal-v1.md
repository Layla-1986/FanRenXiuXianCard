# 正式版第一稿视觉基线

入口：项目根目录 `quota-card-formal-v1.html`，可在本地浏览器打开。

- 第一页：Codex 概念稿 v3，保留青霆玄阵、七日额度、五小时额度与余额布局。
- 第二页：Antigravity 概念稿 v4，保留御气凌霄背景与双模型额度布局。
- 两页共用 480 × 270 逻辑画布；桌面应用会根据 Windows 显示缩放调整原生窗口，使卡片完整显示。
- 点击卡片右上角“引”字按钮往返切换，也可聚焦按钮后用 Enter 或空格操作。
- 此演示稿保留示例数据；实际应用入口为 `quota-card-app.html`，只显示真实接口、有效缓存或明确的不可用状态。
- 两份原稿保持原样。此入口独立于旧版 `start-quota-card.cmd`。

重新生成：`python scripts/build-formal-v1.py`。

验证：正式应用已通过页面测试、视觉规格检查，并在安装目录实际启动验证双页切换、锁定、解锁后拖动与 150% Windows 缩放下的完整显示。
