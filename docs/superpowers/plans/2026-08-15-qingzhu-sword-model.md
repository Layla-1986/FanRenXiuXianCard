# ????????? Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** ????????????????????????????????????????????????????

**Architecture:** ???? v2 ?????? v3 ??????? SVG symbol ?????????????????????? symbol ???????????????????????????????????

**Tech Stack:** HTML?CSS??? SVG??? JavaScript?Node.js ????????

## Global Constraints

- ????????????????????? original-artifact-refined.html?
- ???????????????10/8/5/2 ???????????? 20 ????
- ??? 10 ? 42px?????????????????????????????????
- ??????????????????????

---

### Task 1: ????????

**Files:** Create tests/sword-array-qingzhu-model.test.js; test .superpowers/brainstorm/sword-array-qingzhu-model-v3.html.

**Interfaces:** ???? HTML???????????????????????

- [ ] **Step 1: Write the failing test**

    const test = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('node:fs');
    const path = require('node:path');
    const target = path.join(__dirname, '..', '.superpowers', 'brainstorm', 'sword-array-qingzhu-model-v3.html');
    test('models a layered shared qingzhu sword glyph', () => {
      const html = fs.readFileSync(target, 'utf8');
      for (const token of ['qingzhu-sword-glyph','blade-core','blade-edge','blade-ridge','sword-guard','sword-grip']) assert.match(html,new RegExp(token));
    });
    test('preserves formation behavior', () => {
      const html = fs.readFileSync(target, 'utf8');
      assert.match(html,/animation:swordOrbit 20s linear infinite/);
      assert.match(html,/Math.max/);
      assert.match(html,/i\\*360\\/count/);
      assert.match(html,/width:10px; height:42px/);
    });

- [ ] **Step 2:** ?? node --test tests/sword-array-qingzhu-model.test.js???? v3 ?????????
- [ ] **Step 3:** ???????

### Task 2: ???? SVG ??

**Files:** Create .superpowers/brainstorm/sword-array-qingzhu-model-v3.html.

**Interfaces:** qingzhu-sword-glyph ? render(remain) ??? svg.qingzhu-sword ?????

- [ ] **Step 1:** ?? v2 ??? v3?
- [ ] **Step 2:** ?? blade-core?blade-edge?blade-ridge?sword-guard?sword-grip ?? SVG?
- [ ] **Step 3:** ??????????? transform-origin 50% 75px?????? sword-orbit ???
- [ ] **Step 4:** ??????????? PASS?
- [ ] **Step 5:** ????? v3 ???

### Task 3: ???????

**Files:** Create .superpowers/brainstorm/sword-array-lab/content/sword-array-qingzhu-model-v3.html.

**Interfaces:** ??????? v3 ????????????

- [ ] **Step 1:** ?? v3 ?????????????
- [ ] **Step 2:** ?? 100%?80%?50%?20% ?? 10?8?5?2 ???
- [ ] **Step 3:** ????????????????????????????????
- [ ] **Step 4:** ????????????? PASS?
